import { db } from "@/lib/db";import { DataTable } from "@/components/data-table";export const dynamic="force-dynamic";export default function Page(){const rows=db.prepare(`SELECT id,gateway,event_id,transaction_id,status,processed_at,created_at FROM payment_webhooks ORDER BY created_at DESC LIMIT 500`).all() as Record<string,unknown>[];return <DataTable title="Webhook Payment" rows={rows}/>}
