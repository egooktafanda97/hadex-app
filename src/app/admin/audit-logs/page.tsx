import { db } from "@/lib/db";import { DataTable } from "@/components/data-table";export const dynamic="force-dynamic";export default function Page(){const rows=db.prepare(`SELECT a.id,u.name actor,a.action,a.model_type model,a.model_id,a.created_at dibuat FROM audit_logs a LEFT JOIN users u ON u.id=a.user_id ORDER BY a.created_at DESC LIMIT 500`).all() as Record<string,unknown>[];return <DataTable title="Audit Log" rows={rows}/>}
