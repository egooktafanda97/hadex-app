import { db } from "@/lib/db";import { MasterCrud } from "@/components/crud/master-crud";import { saveLocationAction,toggleActiveAction } from "@/app/actions/master-data";export const dynamic="force-dynamic";export default function Page(){const rows=db.prepare(`SELECT id,name,city,address,is_active FROM locations ORDER BY id DESC`).all() as Record<string,unknown>[];return <MasterCrud title="Lokasi" rows={rows as never[]} path="/admin/locations" table="locations" model="Location" action={saveLocationAction} toggleAction={toggleActiveAction} fields={[{name:"name",label:"Nama",required:true},{name:"city",label:"Kota",required:true},{name:"address",label:"Alamat"},{name:"is_active",label:"Status"}]}/>}
