"use client";import { useState } from "react";export function CheckinForm(){const[result,setResult]=useState("");async function action(formData:FormData){const res=await fetch("/api/checkin",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token:formData.get("token")})});const data=await res.json();setResult(res.ok?`Check-in berhasil: ${data.id}`:data.error)}return <form className="card p-6 max-w-xl grid gap-4" action={action}><label className="label">Token QR<input className="input" name="token" required placeholder="Pindai atau tempel token tiket"/></label><button className="btn btn-primary">Validasi dan check-in</button>{result&&<p>{result}</p>}</form>}
